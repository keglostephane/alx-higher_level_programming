-- Removes all records with a score <= 5 in the table `second_table` of database
-- `hbtn_0c_0`

-- database name will be passed as an argument of mysql command

DELETE FROM second_table WHERE score <= 5
