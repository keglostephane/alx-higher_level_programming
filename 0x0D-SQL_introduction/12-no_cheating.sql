-- Update the score of Bob to 10 in the table `second_table`.

-- update the score of Bob using only the name field.
-- database name will passed as an argument of the mysql command

UPDATE second_table SET score = 10 WHERE name = 'Bob';
